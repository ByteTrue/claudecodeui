---
phase: 03-multi-session-terminal-tabs
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/components/shell/types/types.ts
  - src/components/shell/hooks/useShellConnection.ts
  - src/components/shell/hooks/useShellRuntime.ts
  - src/components/shell/view/Shell.tsx
  - src/components/standalone-shell/view/StandaloneShell.tsx
  - src/components/shell/view/IntegratedTerminalPanel.tsx
  - server/index.js
autonomous: true
requirements:
  - SESS-04
  - SESS-05
user_setup: []
must_haves:
  truths:
    - Every terminal tab has a stable transport identity independent from `projectPath` and `sessionId` collisions.
    - Restarting a tab creates a fresh PTY for that tab instead of silently reconnecting to the old one.
    - Active and inactive tabs can display accurate `connecting`, `live`, `disconnected`, and `exited` status.
  artifacts:
    - Shell websocket init payload includes a tab-scoped identity and restart intent.
    - Server PTY cache keys include terminal-tab identity and emit structured lifecycle events.
    - Shell runtime forwards exit code and lifecycle state into the integrated tab model.
  key_links:
    - `IntegratedTerminalPanel` passes `terminalTabId` and `restartNonce` into `StandaloneShell` / `Shell`.
    - `useShellConnection` sends `terminalTabId` and `forceFresh` in the `init` payload.
    - `server/index.js` uses the tab-aware key and emits lifecycle messages consumed by `Shell`.
---

<objective>
Extend the shell protocol and runtime so terminal tabs have distinct PTY identity, restart semantics, and per-tab lifecycle status.

Purpose: Deliver the backend/runtime contract that makes the Phase 3 UI real instead of cosmetic.
Output: Tab-aware websocket init messages, tab-aware PTY reuse on the server, explicit lifecycle events, and restart behavior that truly replaces a tab's shell session.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-session-terminal-tabs/03-RESEARCH.md
@.planning/phases/03-multi-session-terminal-tabs/03-VALIDATION.md
@.planning/phases/03-multi-session-terminal-tabs/03-01-PLAN.md
@src/components/shell/types/types.ts
@src/components/shell/hooks/useShellConnection.ts
@src/components/shell/hooks/useShellRuntime.ts
@src/components/shell/view/Shell.tsx
@src/components/standalone-shell/view/StandaloneShell.tsx
@src/components/shell/view/IntegratedTerminalPanel.tsx
@server/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend shell message contracts and props for tab identity and exit state</name>
  <files>src/components/shell/types/types.ts, src/components/standalone-shell/view/StandaloneShell.tsx, src/components/shell/view/Shell.tsx, src/components/shell/hooks/useShellRuntime.ts</files>
  <read_first>src/components/shell/types/types.ts, src/components/standalone-shell/view/StandaloneShell.tsx, src/components/shell/view/Shell.tsx, src/components/shell/hooks/useShellRuntime.ts, .planning/phases/03-multi-session-terminal-tabs/03-RESEARCH.md</read_first>
  <action>In `src/components/shell/types/types.ts`, extend `ShellStatusPhase` to `loading | connecting | live | disconnected | exited`. Extend `ShellStatusSnapshot` with `exitCode: number | null`. Add `terminalTabId: string` and `forceFresh: boolean` to `ShellInitMessage`. Add new incoming message types `{ type: 'status', phase, terminalTabId, canRetry? }` and `{ type: 'process_exit', terminalTabId, exitCode, signal? }`. In `StandaloneShell.tsx` and `Shell.tsx`, add props named `terminalTabId` and `restartNonce`. In `useShellRuntime.ts`, replace `lastSessionIdRef` with a `lastTabIdentityRef` built from `terminalTabId` and `restartNonce` so runtime identity changes can be keyed to the tab rather than only to `selectedSession?.id`.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/components/shell/types/types.ts` contains `export type ShellStatusPhase = 'loading' | 'connecting' | 'live' | 'disconnected' | 'exited'`
    - `src/components/shell/types/types.ts` contains `exitCode: number | null`
    - `src/components/shell/types/types.ts` contains `terminalTabId: string`
    - `src/components/shell/types/types.ts` contains `forceFresh: boolean`
    - `src/components/standalone-shell/view/StandaloneShell.tsx` contains `terminalTabId`
    - `src/components/shell/view/Shell.tsx` contains `restartNonce`
    - `src/components/shell/hooks/useShellRuntime.ts` contains `lastTabIdentityRef`
  </acceptance_criteria>
  <done>The shell runtime and message types can now represent tab identity, restart intent, and exited state explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Make websocket init and PTY reuse tab-aware, with true restart semantics</name>
  <files>src/components/shell/hooks/useShellConnection.ts, server/index.js</files>
  <read_first>src/components/shell/hooks/useShellConnection.ts, server/index.js, src/components/shell/types/types.ts, .planning/phases/03-multi-session-terminal-tabs/03-RESEARCH.md</read_first>
  <action>In `src/components/shell/hooks/useShellConnection.ts`, add a `lastInitRestartNonceRef` initialized as `useRef(restartNonce)` and send `terminalTabId` plus `forceFresh` in the `init` payload. `forceFresh` must be true only when `restartNonce !== lastInitRestartNonceRef.current`; after sending `init`, update `lastInitRestartNonceRef.current = restartNonce`. This contract depends on tab switching remounting the shell by `activeTab.id` only, so each mounted tab starts with its own restart baseline while a same-tab restart changes `restartNonce` inside that mounted instance. In `server/index.js`, validate `terminalTabId` with the same safe-character pattern as `sessionId`. Build `ptySessionKey` as `${projectPath}_${sessionId || 'default'}_${terminalTabId}${commandSuffix}`. If `forceFresh` is true and an existing PTY exists for that key, kill it, clear any timeout, and delete it before spawning the new PTY. Emit `{ type: 'status', phase: 'connecting', terminalTabId }` before spawning, `{ type: 'status', phase: 'live', terminalTabId }` after a fresh spawn or reconnect, `{ type: 'status', phase: 'disconnected', terminalTabId }` when the websocket closes but the PTY is being retained, and `{ type: 'process_exit', terminalTabId, exitCode, signal }` inside `onExit`.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/components/shell/hooks/useShellConnection.ts` contains `terminalTabId`
    - `src/components/shell/hooks/useShellConnection.ts` contains `forceFresh`
    - `src/components/shell/hooks/useShellConnection.ts` contains `lastInitRestartNonceRef`
    - `src/components/shell/hooks/useShellConnection.ts` contains `useRef(restartNonce)`
    - `server/index.js` contains `terminalTabId`
    - `server/index.js` contains ``${projectPath}_${sessionId || 'default'}_${terminalTabId}``
    - `server/index.js` contains `forceFresh`
    - `server/index.js` contains `type: 'process_exit'`
  </acceptance_criteria>
  <done>The backend can distinguish tabs, preserve inactive PTYs by tab id, and create a genuinely fresh shell when a tab is restarted.</done>
</task>

<task type="auto">
  <name>Task 3: Feed lifecycle events back into the integrated tab model</name>
  <files>src/components/shell/view/Shell.tsx, src/components/standalone-shell/view/StandaloneShell.tsx, src/components/shell/view/IntegratedTerminalPanel.tsx</files>
  <read_first>src/components/shell/view/Shell.tsx, src/components/standalone-shell/view/StandaloneShell.tsx, src/components/shell/view/IntegratedTerminalPanel.tsx, src/components/shell/hooks/useShellConnection.ts, src/hooks/useProjectsState.ts</read_first>
  <action>In `Shell.tsx`, update the local shell-status reducer so `process_exit` sets `phase: 'exited'`, `exitCode` from the server payload, and `canRetry: true`; a later `status.phase = 'live'` must clear `exitCode` back to `null`. Forward the resulting `ShellStatusSnapshot` through `onStatusChange`. In `StandaloneShell.tsx`, pass `terminalTabId` and `restartNonce` to `Shell`. In `IntegratedTerminalPanel.tsx`, map the runtime-only shell phase `loading` to the persisted tab-safe value `connecting` before calling `onTerminalTabStatusChange`. The exact payload shape must be `{ status: shellStatus.phase === 'loading' ? 'connecting' : shellStatus.phase, canRetry: shellStatus.canRetry, exitCode: shellStatus.exitCode }`, so `IntegratedTerminalTab.status` never stores `loading`. Ensure the active tab restart button keeps calling `onRestartTerminalTab(activeTab.id)`.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/components/shell/view/Shell.tsx` contains `phase: 'exited'`
    - `src/components/shell/view/Shell.tsx` clears `exitCode` on live status
    - `src/components/standalone-shell/view/StandaloneShell.tsx` passes `terminalTabId={terminalTabId}`
    - `src/components/standalone-shell/view/StandaloneShell.tsx` passes `restartNonce={restartNonce}`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `shellStatus.phase === 'loading' ? 'connecting' : shellStatus.phase`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `status: shellStatus.phase === 'loading' ? 'connecting' : shellStatus.phase`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` still calls `onRestartTerminalTab(activeTab.id)`
  </acceptance_criteria>
  <done>Tab lifecycle chips now reflect real server/runtime state, including fresh restart and clean exited status.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint && npm run typecheck
- [ ] Creating two tabs in the same project produces two distinct PTY identities
- [ ] Restarting one tab creates a fresh shell for that tab only
- [ ] Tab chips can display `connecting`, `live`, `disconnected`, and `exited`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The shell transport contract is tab-aware
- Restart is a true tab-scoped restart rather than a reconnect to the old PTY
- Per-tab lifecycle state is available to the integrated panel
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-session-terminal-tabs/03-02-SUMMARY.md`
</output>
