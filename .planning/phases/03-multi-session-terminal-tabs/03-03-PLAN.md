---
phase: 03-multi-session-terminal-tabs
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/hooks/useProjectsState.ts
  - src/components/main-content/view/MainContent.tsx
  - src/components/shell/view/IntegratedTerminalPanel.tsx
  - src/components/standalone-shell/view/StandaloneShell.tsx
  - src/i18n/locales/zh-CN/chat.json
  - src/i18n/locales/de/chat.json
  - src/i18n/locales/ja/chat.json
  - src/i18n/locales/ko/chat.json
  - src/i18n/locales/ru/chat.json
autonomous: true
requirements:
  - SESS-02
  - SESS-05
user_setup: []
must_haves:
  truths:
    - Switching tabs changes the active shell without restarting the inactive tabs' PTYs.
    - Closing, reopening, and reselecting tabs preserves predictable active-tab behavior.
    - Inactive tabs keep their own last known lifecycle state instead of mirroring the active tab.
  artifacts:
    - The active shell mount is keyed by `activeTab.id`, while `restartNonce` stays a prop-driven restart signal inside the mounted shell.
    - Tab-close and panel-reopen helpers preserve `activeTabId` rules consistently.
    - Status updates are scoped to the matching tab record only.
  key_links:
    - `MainContent` resolves the active tab from `terminalPanelState.tabs` and `activeTabId`.
    - `IntegratedTerminalPanel` mounts the visible shell with `key=terminal-shell-${activeTab.id}` and passes `restartNonce` through to the mounted shell instance for same-tab restart.
    - `useProjectsState` updates only the targeted tab when status, restart, or close actions occur.
---

<objective>
Harden tab switching and panel continuity so inactive terminal sessions remain intact while the active tab changes.

Purpose: Finish the behavioral core of Phase 3 by ensuring the tab model survives real user flow instead of only looking correct in code.
Output: Active-shell remounting keyed to tab identity, stable close/reopen rules, and tab-scoped status updates that preserve inactive sessions.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-session-terminal-tabs/03-RESEARCH.md
@.planning/phases/03-multi-session-terminal-tabs/03-VALIDATION.md
@.planning/phases/03-multi-session-terminal-tabs/03-01-PLAN.md
@.planning/phases/03-multi-session-terminal-tabs/03-02-PLAN.md
@src/hooks/useProjectsState.ts
@src/components/main-content/view/MainContent.tsx
@src/components/shell/view/IntegratedTerminalPanel.tsx
@src/components/standalone-shell/view/StandaloneShell.tsx
@src/components/shell/hooks/useShellRuntime.ts
@server/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Key the visible shell by active tab identity so tab switches reconnect to the correct PTY</name>
  <files>src/components/main-content/view/MainContent.tsx, src/components/shell/view/IntegratedTerminalPanel.tsx, src/components/standalone-shell/view/StandaloneShell.tsx</files>
  <read_first>src/components/main-content/view/MainContent.tsx, src/components/shell/view/IntegratedTerminalPanel.tsx, src/components/standalone-shell/view/StandaloneShell.tsx, src/components/shell/hooks/useShellRuntime.ts, server/index.js</read_first>
  <action>In `MainContent.tsx`, derive `activeTerminalTab` once and pass only that tab's binding-derived `boundProject` and `boundSession` into `IntegratedTerminalPanel`. In `IntegratedTerminalPanel.tsx`, render the active shell with the exact React key ``terminal-shell-${activeTab.id}`` and pass `terminalTabId={activeTab.id}` plus `restartNonce={activeTab.restartNonce}` into `StandaloneShell`. Do not rely on `selectedSession?.id` or ambient project changes alone to swap shell runtime identity. This keyed remount is mandatory so switching between two tabs with the same `sessionId` or `null` session ids still reconnects to the correct PTY, while same-tab restart remains a prop-driven `restartNonce` change inside the mounted shell instance.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/components/main-content/view/MainContent.tsx` contains `activeTerminalTab`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains ``terminal-shell-${activeTab.id}``
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` passes `terminalTabId={activeTab.id}`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` passes `restartNonce={activeTab.restartNonce}`
  </acceptance_criteria>
  <done>The visible shell runtime now follows terminal-tab identity directly, so switching tabs reattaches to the correct PTY even when session ids overlap.</done>
</task>

<task type="auto">
  <name>Task 2: Make close, reopen, and selection behavior deterministic for multi-tab state</name>
  <files>src/hooks/useProjectsState.ts, src/components/shell/view/IntegratedTerminalPanel.tsx</files>
  <read_first>src/hooks/useProjectsState.ts, src/components/shell/view/IntegratedTerminalPanel.tsx, .planning/phases/03-multi-session-terminal-tabs/03-RESEARCH.md</read_first>
  <action>Refine the tab helpers in `src/hooks/useProjectsState.ts` so `setActiveTerminalTab` updates `activeTabId`, preserves `isOpen: true`, and increments `focusVersion` only when switching to a different tab. Keep `openTerminalPanel` idempotent when tabs already exist: it must reopen the panel without creating duplicates. Keep `closeTerminalTab` behavior exact: choose the left neighbor when closing the active tab and a left neighbor exists, otherwise the first remaining tab, and if no tabs remain set `isOpen: false`, `activeTabId: null`, and leave `height` unchanged. In `IntegratedTerminalPanel.tsx`, make all close/select button handlers call these helpers directly from the tab strip so there is no secondary local tab state.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/hooks/useProjectsState.ts` contains `setActiveTerminalTab`
    - `src/hooks/useProjectsState.ts` contains `focusVersion: previous.focusVersion + 1`
    - `src/hooks/useProjectsState.ts` contains `activeTabId: null`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `onSelectTerminalTab(tab.id)`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `onCloseTerminalTab(tab.id)`
  </acceptance_criteria>
  <done>Tab selection and close/reopen flow are now deterministic and reusable instead of depending on incidental component state.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve inactive-tab lifecycle state instead of mirroring the active shell</name>
  <files>src/hooks/useProjectsState.ts, src/components/shell/view/IntegratedTerminalPanel.tsx, src/i18n/locales/zh-CN/chat.json, src/i18n/locales/de/chat.json, src/i18n/locales/ja/chat.json, src/i18n/locales/ko/chat.json, src/i18n/locales/ru/chat.json</files>
  <read_first>src/hooks/useProjectsState.ts, src/components/shell/view/IntegratedTerminalPanel.tsx, src/components/shell/view/Shell.tsx, .planning/phases/03-multi-session-terminal-tabs/03-VALIDATION.md, src/i18n/locales/zh-CN/chat.json, src/i18n/locales/de/chat.json, src/i18n/locales/ja/chat.json, src/i18n/locales/ko/chat.json, src/i18n/locales/ru/chat.json</read_first>
  <action>In `src/hooks/useProjectsState.ts`, make `updateTerminalTabStatus` update only the matching tab by id, leaving every other tab record untouched. Preserve the previous `title`, `binding`, and `restartNonce` when status changes; only update `status`, `canRetry`, `exitCode`, and `updatedAt`. In `IntegratedTerminalPanel.tsx`, render each inactive tab's chip from its stored record, not from the active shell's local state, and do not reset non-active tabs to `connecting` during tab switches, panel reopen, or active-tab restart unless that specific tab receives a mapped status update. As the low-risk localization tail for this phase, fan the already-established English keys into `zh-CN`, `de`, `ja`, `ko`, and `ru` under `chat.shell.tabs` / `chat.shell.header` so the tab strip and status chips do not rely on English fallback outside the initial implementation pass.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/hooks/useProjectsState.ts` contains `tab.id === targetTabId`
    - `src/hooks/useProjectsState.ts` preserves `title: tab.title` or leaves `title` unchanged in the status updater
    - `src/hooks/useProjectsState.ts` updates `updatedAt: Date.now()`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` renders inactive tab chips from `tab.status`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` does not set all tabs to `connecting` inside tab-switch handlers
    - `src/i18n/locales/zh-CN/chat.json` contains `shell.tabs.newTab`
    - `src/i18n/locales/de/chat.json` contains `shell.header.statusExited`
    - `src/i18n/locales/ja/chat.json` contains `shell.tabs.restartTab`
    - `src/i18n/locales/ko/chat.json` contains `shell.header.statusLive`
    - `src/i18n/locales/ru/chat.json` contains `shell.tabs.closeTab`
  </acceptance_criteria>
  <done>Inactive tabs keep their own lifecycle history, which makes tab switching trustworthy and keeps status chips meaningful.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint && npm run typecheck
- [ ] Switching tabs does not restart an inactive tab's PTY
- [ ] Closing the active tab selects the expected remaining tab or closes the panel only when no tabs remain
- [ ] Inactive tabs keep their own status chips after active-tab changes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Inactive tabs remain intact while the active tab changes
- Close/reopen logic is predictable for the user
- Per-tab status remains scoped to the correct tab record
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-session-terminal-tabs/03-03-SUMMARY.md`
</output>
