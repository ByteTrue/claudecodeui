---
phase: 03-multi-session-terminal-tabs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/app.ts
  - src/hooks/useProjectsState.ts
  - src/components/app/AppContent.tsx
  - src/components/main-content/types/types.ts
  - src/components/main-content/view/MainContent.tsx
  - src/components/shell/view/IntegratedTerminalPanel.tsx
  - src/i18n/locales/en/chat.json
autonomous: true
requirements:
  - SESS-01
  - SESS-03
  - SESS-04
  - SESS-05
user_setup: []
must_haves:
  truths:
    - The integrated terminal panel owns an explicit tab list plus active-tab selection instead of a single binding.
    - Users can create, select, close, and request restart for terminal tabs directly from the panel UI.
    - Every visible terminal tab shows a stable title and a stored lifecycle status without depending on the current workspace selection.
  artifacts:
    - `TerminalPanelState` contains `tabs` and `activeTabId` plus per-tab lifecycle fields.
    - `useProjectsState` exposes tab create/select/close/restart/status-update helpers.
    - `IntegratedTerminalPanel` renders a tab strip with add, close, restart, and status affordances.
  key_links:
    - `useProjectsState` builds terminal tab records from the current project/session/provider selection.
    - `AppContent` and `MainContent` pass tab state and tab actions into `IntegratedTerminalPanel`.
    - `IntegratedTerminalPanel` renders controls from stored tab state rather than deriving everything from the active shell alone.
---

<objective>
Establish the frontend terminal-tab state model and the integrated panel controls for tab lifecycle.

Purpose: Deliver the visible Phase 3 shell chrome so the panel can hold multiple terminal identities and the user can create, choose, close, and restart them without leaving the integrated workflow.
Output: Tab-aware terminal panel state, app-level wiring for tab actions, and a tab strip that surfaces stable labels plus status chips.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-session-terminal-tabs/03-RESEARCH.md
@.planning/phases/03-multi-session-terminal-tabs/03-VALIDATION.md
@.planning/phases/02-project-aware-terminal-launch/02-VERIFICATION.md
@src/types/app.ts
@src/hooks/useProjectsState.ts
@src/components/app/AppContent.tsx
@src/components/main-content/types/types.ts
@src/components/main-content/view/MainContent.tsx
@src/components/shell/view/IntegratedTerminalPanel.tsx
@src/i18n/locales/en/chat.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the single terminal binding with an explicit tab state model</name>
  <files>src/types/app.ts, src/hooks/useProjectsState.ts</files>
  <read_first>src/types/app.ts, src/hooks/useProjectsState.ts, .planning/phases/03-multi-session-terminal-tabs/03-RESEARCH.md, .planning/phases/02-project-aware-terminal-launch/02-VERIFICATION.md</read_first>
  <action>In `src/types/app.ts`, add `export type TerminalTabStatus = 'connecting' | 'live' | 'disconnected' | 'exited'` and `export interface IntegratedTerminalTab` with the exact fields `id`, `title`, `binding`, `status`, `canRetry`, `exitCode`, `restartNonce`, `createdAt`, and `updatedAt`. Replace `TerminalPanelState.binding` with `tabs: IntegratedTerminalTab[]` and `activeTabId: string | null`, while preserving `isOpen`, `height`, and `focusVersion`. In `src/hooks/useProjectsState.ts`, add a helper that builds a tab from the current workspace selection using `window.crypto.randomUUID()`, `selectedProject.displayName` as the base title, `selectedProject.fullPath || selectedProject.path || ''` as `binding.projectPath`, `selectedSession?.id || null`, `selectedSession?.__provider || readPersistedProvider()`, initial `status: 'connecting'`, `canRetry: false`, `exitCode: null`, `restartNonce: 0`, and `Date.now()` timestamps. Append ` · 2`, ` · 3`, and so on to duplicate titles. Update `openTerminalPanel` so it creates the first tab only when `previous.tabs.length === 0`; otherwise it only opens/focuses the existing active tab. Add tab helpers named `createTerminalTab`, `setActiveTerminalTab`, `closeTerminalTab`, `restartTerminalTab`, and `updateTerminalTabStatus`. Define `restartTerminalTab(tabId)` explicitly: increment the target tab's `restartNonce`, set `activeTabId` to `tabId`, set that tab's `status` to `connecting`, clear `exitCode` to `null`, set `canRetry: false`, update `updatedAt: Date.now()`, preserve `title` and `binding`, and increment `focusVersion` so the restarted shell refocuses. `closeTerminalTab` must select the immediate left neighbor when the active tab is closed, otherwise the first remaining tab, and closing the last tab must set `isOpen: false` and `activeTabId: null`.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/types/app.ts` contains `export type TerminalTabStatus = 'connecting' | 'live' | 'disconnected' | 'exited'`
    - `src/types/app.ts` contains `export interface IntegratedTerminalTab`
    - `src/types/app.ts` contains `tabs: IntegratedTerminalTab[]`
    - `src/types/app.ts` contains `activeTabId: string | null`
    - `src/hooks/useProjectsState.ts` contains `createTerminalTab`
    - `src/hooks/useProjectsState.ts` contains `updateTerminalTabStatus`
    - `src/hooks/useProjectsState.ts` contains `restartTerminalTab`
    - `src/hooks/useProjectsState.ts` contains `restartNonce: tab.restartNonce + 1`
    - `src/hooks/useProjectsState.ts` contains `status: 'connecting'`
    - `src/hooks/useProjectsState.ts` contains `canRetry: false`
    - `src/hooks/useProjectsState.ts` contains `exitCode: null`
    - `src/hooks/useProjectsState.ts` contains `window.crypto.randomUUID()`
  </acceptance_criteria>
  <done>The integrated terminal can now represent multiple tab identities in app state instead of one global binding.</done>
</task>

<task type="auto">
  <name>Task 2: Thread terminal-tab state and actions through the workspace layout</name>
  <files>src/components/app/AppContent.tsx, src/components/main-content/types/types.ts, src/components/main-content/view/MainContent.tsx</files>
  <read_first>src/components/app/AppContent.tsx, src/components/main-content/types/types.ts, src/components/main-content/view/MainContent.tsx, src/hooks/useProjectsState.ts, src/types/app.ts</read_first>
  <action>Expose the new tab helpers from `useProjectsState.ts` through `AppContent.tsx` into `MainContent`. Extend `MainContentProps` with the exact callbacks `onNewTerminalTab`, `onSelectTerminalTab`, `onCloseTerminalTab`, `onRestartTerminalTab`, and `onTerminalTabStatusChange`. Define `onTerminalTabStatusChange` to accept `(tabId: string, nextStatus: { status: TerminalTabStatus; canRetry: boolean; exitCode: number | null }) => void`, so persisted tab records only store the tab-safe status contract from `src/types/app.ts`. In `MainContent.tsx`, replace the old `terminalPanelState.binding` derivation with `const activeTerminalTab = terminalPanelState.tabs.find((tab) => tab.id === terminalPanelState.activeTabId) ?? terminalPanelState.tabs[0] ?? null;`. Derive `boundProject` and `boundSession` from `activeTerminalTab.binding`, not from ambient workspace selection. Pass `terminalTabs={terminalPanelState.tabs}`, `activeTerminalTabId={activeTerminalTab?.id ?? null}`, and all tab callbacks into `IntegratedTerminalPanel`. Preserve `selectedProject` as the current viewed project only; it must not become the source of terminal identity when tabs already exist.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/components/main-content/types/types.ts` contains `onNewTerminalTab`
    - `src/components/main-content/types/types.ts` contains `onTerminalTabStatusChange`
    - `src/components/main-content/types/types.ts` contains `status: TerminalTabStatus`
    - `src/components/main-content/types/types.ts` contains `exitCode: number | null`
    - `src/components/app/AppContent.tsx` passes `onNewTerminalTab={createTerminalTab}`
    - `src/components/main-content/view/MainContent.tsx` contains `activeTerminalTab = terminalPanelState.tabs.find`
    - `src/components/main-content/view/MainContent.tsx` passes `terminalTabs={terminalPanelState.tabs}`
    - `src/components/main-content/view/MainContent.tsx` passes `activeTerminalTabId={activeTerminalTab?.id ?? null}`
  </acceptance_criteria>
  <done>The app layout can now address the active terminal tab and invoke tab lifecycle actions from the integrated panel.</done>
</task>

<task type="auto">
  <name>Task 3: Render the integrated terminal tab strip with create, close, restart, and status affordances</name>
  <files>src/components/shell/view/IntegratedTerminalPanel.tsx, src/i18n/locales/en/chat.json</files>
  <read_first>src/components/shell/view/IntegratedTerminalPanel.tsx, .planning/phases/03-multi-session-terminal-tabs/03-RESEARCH.md, src/i18n/locales/en/chat.json</read_first>
  <action>Update `IntegratedTerminalPanel.tsx` so it accepts `terminalTabs`, `activeTerminalTabId`, `onNewTerminalTab`, `onSelectTerminalTab`, `onCloseTerminalTab`, `onRestartTerminalTab`, and `onTerminalTabStatusChange`. Render a horizontal tab strip above the shell body. Each tab button must show `tab.title`, a lifecycle chip derived from `tab.status`, and close affordance. Add a single plus button that calls `onNewTerminalTab`, and make the active tab expose a restart affordance that calls `onRestartTerminalTab(activeTab.id)`. Keep the main header title fixed to `Terminal`; do not bring back the heavy standalone shell header. Add the new tab-strip keys under `chat.shell.tabs` / `chat.shell.header` in `src/i18n/locales/en/chat.json` only for this plan, and use `t(..., { defaultValue: ... })` fallback strings anywhere the component would otherwise depend on untranslated locale files. Non-English locale fan-out is the low-risk tail of Phase 3 and belongs to `03-03` after tab behavior is stable.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `terminalTabs`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `activeTerminalTabId`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `onNewTerminalTab`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `onRestartTerminalTab`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `tab.status`
    - `src/i18n/locales/en/chat.json` contains `shell.tabs.newTab`
    - `src/i18n/locales/en/chat.json` contains `shell.header.statusExited`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `defaultValue:`
  </acceptance_criteria>
  <done>The integrated panel shows tab lifecycle controls and per-tab status directly in the workspace UI.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint && npm run typecheck
- [ ] User can create a second terminal tab from the integrated panel
- [ ] User can close an individual tab without collapsing the whole panel unless it was the last tab
- [ ] Active tab exposes a restart affordance and every tab shows a stored lifecycle chip
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The integrated terminal panel owns multiple tab identities instead of one binding
- Users can create, select, close, and request restart from the tab strip
- Each tab has a stable label and visible status chip
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-session-terminal-tabs/03-01-SUMMARY.md`
</output>
