---
phase: 02-project-aware-terminal-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/app.ts
  - src/hooks/useProjectsState.ts
  - src/components/main-content/types/types.ts
  - src/components/app/AppContent.tsx
  - src/components/main-content/view/MainContent.tsx
  - src/components/shell/view/IntegratedTerminalPanel.tsx
autonomous: true
requirements:
  - INTE-04
  - TERM-01
user_setup: []
must_haves:
  truths:
    - Opening the integrated terminal snapshots the current project/workspace launch context instead of following later workspace or session selection changes.
    - Closing and reopening the panel restores the same bound terminal context while the underlying shell session is still alive.
    - Normal project/session navigation no longer tears down the integrated terminal unless the user intentionally replaces its launch context.
  artifacts:
    - Terminal panel state includes a bound launch context with project path and optional session/provider snapshot.
    - MainContent receives enough data to distinguish the current viewed project from the terminal-bound project.
    - IntegratedTerminalPanel renders the shell against a bound project/session pair rather than ambient selection props.
  key_links:
    - useProjectsState captures terminal binding on open and preserves it across close/reopen.
    - AppContent and MainContent thread the bound context separately from the ambient workspace selection.
    - IntegratedTerminalPanel launches StandaloneShell with the same bound `projectPath` / session snapshot so the existing `/shell` reconnect path can reuse the live PTY session.
---

<objective>
Establish a stable terminal launch binding that is independent from ongoing workspace navigation.

Purpose: Deliver the Phase 2 core behavior that a live integrated terminal belongs to one project/workspace context until the user intentionally replaces it.
Output: Bound terminal launch state in workspace state, explicit prop threading for current-vs-bound project context, and integrated shell mounting that uses the bound launch snapshot instead of live sidebar selection.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-project-aware-terminal-launch/02-CONTEXT.md
@.planning/phases/02-project-aware-terminal-launch/02-RESEARCH.md
@.planning/phases/02-project-aware-terminal-launch/02-VALIDATION.md
@src/types/app.ts
@src/hooks/useProjectsState.ts
@src/components/app/AppContent.tsx
@src/components/main-content/types/types.ts
@src/components/main-content/view/MainContent.tsx
@src/components/shell/view/IntegratedTerminalPanel.tsx
@src/components/standalone-shell/view/StandaloneShell.tsx
@src/components/shell/view/Shell.tsx
@server/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bound launch context to terminal panel state</name>
  <files>src/types/app.ts, src/hooks/useProjectsState.ts, src/components/main-content/types/types.ts</files>
  <read_first>src/types/app.ts, src/hooks/useProjectsState.ts, src/components/main-content/types/types.ts, .planning/phases/02-project-aware-terminal-launch/02-CONTEXT.md</read_first>
  <action>Add `TerminalBindingContext` to `src/types/app.ts` with the exact fields `projectName`, `projectDisplayName`, `projectPath`, `sessionId`, and `provider`. Extend `TerminalPanelState` so it carries `binding: TerminalBindingContext | null` in addition to `isOpen`, `height`, and `focusVersion`. In `src/hooks/useProjectsState.ts`, update `openTerminalPanel` so it snapshots the current launch inputs only when `previous.binding` is null, using `selectedProject.name`, `selectedProject.displayName`, `selectedProject.fullPath || selectedProject.path || ''`, `selectedSession?.id || null`, and `selectedSession?.__provider || (localStorage.getItem('selected-provider') || 'claude')`. Make `closeTerminalPanel` preserve binding explicitly by returning `binding: previous.binding` alongside `isOpen: false`, and do not clear `binding` in `handleProjectSelect`, `handleSessionSelect`, `handleNewSession`, or background refresh logic.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/types/app.ts` contains `export interface TerminalBindingContext`
    - `src/types/app.ts` contains `binding: TerminalBindingContext | null`
    - `src/hooks/useProjectsState.ts` contains `previous.binding` in `openTerminalPanel`
    - `src/hooks/useProjectsState.ts` contains `binding: previous.binding` inside `closeTerminalPanel`
  </acceptance_criteria>
  <done>Workspace state can distinguish between terminal visibility and the persistent launch context that the integrated terminal belongs to.</done>
</task>

<task type="auto">
  <name>Task 2: Thread current-vs-bound terminal context through MainContent</name>
  <files>src/components/app/AppContent.tsx, src/components/main-content/types/types.ts, src/components/main-content/view/MainContent.tsx</files>
  <read_first>src/components/app/AppContent.tsx, src/components/main-content/types/types.ts, src/components/main-content/view/MainContent.tsx, src/hooks/useProjectsState.ts, src/types/app.ts</read_first>
  <action>Pass `projects` from `useProjectsState` into `MainContent` so the workspace layout can resolve a terminal-bound project independently from `selectedProject`. Extend `MainContentProps` with `projects: Project[]`. In `MainContent.tsx`, derive `boundProject` by matching `terminalPanelState.binding?.projectName` against the loaded `projects` array; if no match exists, build a minimal `Project` object from the binding snapshot using `name`, `displayName`, `fullPath`, and `path`. Also derive `boundSession` from the binding snapshot: when `binding.sessionId` is present, resolve a matching session from the bound project if possible, otherwise build a minimal `{ id, __provider }` session object from the snapshot. Keep `selectedProject` as the current viewed project, and do not let `selectedProject` or `selectedSession` replace the bound values just because sidebar navigation changed.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/components/main-content/types/types.ts` contains `projects: Project[]`
    - `src/components/app/AppContent.tsx` passes `projects={projects}` into `MainContent`
    - `src/components/main-content/view/MainContent.tsx` contains a `boundProject` derivation based on `terminalPanelState.binding`
    - `src/components/main-content/view/MainContent.tsx` contains a `boundSession` derivation based on `terminalPanelState.binding`
  </acceptance_criteria>
  <done>MainContent has both the current viewed project and the terminal-bound launch context available at the same time.</done>
</task>

<task type="auto">
  <name>Task 3: Launch the integrated shell from the bound context instead of ambient selection</name>
  <files>src/components/main-content/view/MainContent.tsx, src/components/shell/view/IntegratedTerminalPanel.tsx</files>
  <read_first>src/components/main-content/view/MainContent.tsx, src/components/shell/view/IntegratedTerminalPanel.tsx, src/components/standalone-shell/view/StandaloneShell.tsx, src/components/shell/view/Shell.tsx, server/index.js</read_first>
  <action>Change `IntegratedTerminalPanel` to accept `currentProject`, `terminalBinding`, `boundProject`, and `boundSession` as separate inputs. Pass `project={boundProject}` and `session={boundSession}` into `StandaloneShell` so the integrated terminal keeps using the bound launch snapshot on reconnect. Do not pass ambient `selectedProject` or `selectedSession` straight through to the shell mount anymore. Preserve the existing `isOpen`, `focusVersion`, and auto-connect behavior so reopening the panel reuses the same `projectPath_sessionId` identity that `server/index.js` already keys in `ptySessionsMap`.</action>
  <verify>npm run lint && npm run typecheck</verify>
  <acceptance_criteria>
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains a `terminalBinding` prop
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `project={boundProject}`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `session={boundSession}`
    - `src/components/main-content/view/MainContent.tsx` contains `currentProject={selectedProject}`
    - `src/components/main-content/view/MainContent.tsx` contains `boundProject={boundProject}`
    - `src/components/main-content/view/MainContent.tsx` contains `boundSession={boundSession}`
  </acceptance_criteria>
  <done>The integrated terminal launches and reconnects from a stable bound project/session snapshot instead of following whatever the workspace selected most recently.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint && npm run typecheck
- [ ] Opening the integrated terminal snapshots launch context once and preserves it across close/reopen
- [ ] Switching projects or sessions in the workspace does not silently retarget the integrated terminal
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The integrated terminal owns a stable bound launch context
- Reopen behavior preserves the existing live terminal identity
- Ambient workspace navigation no longer restarts the integrated terminal unexpectedly
</success_criteria>

<output>
After completion, create `.planning/phases/02-project-aware-terminal-launch/02-01-SUMMARY.md`
</output>
