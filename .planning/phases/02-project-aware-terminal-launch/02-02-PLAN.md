---
phase: 02-project-aware-terminal-launch
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/components/shell/types/types.ts
  - src/components/shell/view/Shell.tsx
  - src/components/standalone-shell/view/StandaloneShell.tsx
  - src/components/shell/view/IntegratedTerminalPanel.tsx
  - src/i18n/locales/en/chat.json
  - src/i18n/locales/zh-CN/chat.json
  - src/i18n/locales/de/chat.json
  - src/i18n/locales/ja/chat.json
  - src/i18n/locales/ko/chat.json
  - src/i18n/locales/ru/chat.json
autonomous: true
requirements:
  - INTE-05
  - TERM-02
user_setup: []
must_haves:
  truths:
    - The integrated terminal header always shows which project/workspace the terminal belongs to.
    - When the current viewed project differs from the bound terminal project, the panel surfaces that mismatch clearly but lightly.
    - Terminal viewport sizing remains correct after the richer header is added and after the panel is reopened or resized.
  artifacts:
    - Shell runtime can report a compact connection-status snapshot to the integrated panel.
    - IntegratedTerminalPanel renders bound project identity, a mismatch cue, and lightweight live/disconnected state.
    - The shell body region uses flexible `min-h-0` layout instead of a fixed height calculation tied to the old header size.
  key_links:
    - Shell and StandaloneShell propagate runtime status into IntegratedTerminalPanel.
    - IntegratedTerminalPanel compares `currentProject` with `terminalBinding.projectName` to render mismatch UI.
    - The panel body layout keeps xterm inside a flex-1 / min-h-0 chain so ResizeObserver-driven fitting still works when header height changes.
---

<objective>
Surface bound project identity in the integrated terminal UI and keep the terminal viewport stable under the richer header.

Purpose: Finish the user-facing part of Phase 2 so people can see which workspace the terminal belongs to and keep prompt/output layout correct while the panel remains a live terminal.
Output: Compact runtime-status plumbing, a project-aware integrated header, and flex-based terminal body sizing that stays correct after header growth, reopen, and resize.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-project-aware-terminal-launch/02-CONTEXT.md
@.planning/phases/02-project-aware-terminal-launch/02-RESEARCH.md
@.planning/phases/02-project-aware-terminal-launch/02-VALIDATION.md
@.planning/phases/02-project-aware-terminal-launch/02-01-PLAN.md
@src/components/shell/types/types.ts
@src/components/shell/view/Shell.tsx
@src/components/standalone-shell/view/StandaloneShell.tsx
@src/components/shell/view/IntegratedTerminalPanel.tsx
@src/components/shell/hooks/useShellRuntime.ts
@src/components/shell/hooks/useShellTerminal.ts
@src/i18n/locales/en/chat.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose compact shell status to the integrated panel</name>
  <files>src/components/shell/types/types.ts, src/components/shell/view/Shell.tsx, src/components/standalone-shell/view/StandaloneShell.tsx</files>
  <read_first>src/components/shell/types/types.ts, src/components/shell/view/Shell.tsx, src/components/standalone-shell/view/StandaloneShell.tsx, src/components/shell/hooks/useShellRuntime.ts, src/components/shell/view/IntegratedTerminalPanel.tsx</read_first>
  <action>Add `ShellStatusPhase = 'loading' | 'connecting' | 'live' | 'disconnected'` and a `ShellStatusSnapshot` type to `src/components/shell/types/types.ts`. Add an optional `onStatusChange` prop to `Shell` and `StandaloneShell`. In `Shell.tsx`, derive the snapshot from existing runtime state with the exact mapping `!isInitialized -> 'loading'`, `isConnecting -> 'connecting'`, `isConnected -> 'live'`, otherwise `'disconnected'`, and set `canRetry` true only when the shell is initialized but not connected/connecting. Emit the snapshot from an effect whenever status changes. Keep `ShellConnectionOverlay` as the explicit retry surface by preserving `onConnect={connectToShell}` instead of adding persistent reconnect controls.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/components/shell/types/types.ts` contains `export type ShellStatusPhase = 'loading' | 'connecting' | 'live' | 'disconnected'`
    - `src/components/shell/types/types.ts` contains `export type ShellStatusSnapshot`
    - `src/components/shell/view/Shell.tsx` contains `onStatusChange`
    - `src/components/standalone-shell/view/StandaloneShell.tsx` forwards `onStatusChange`
  </acceptance_criteria>
  <done>The integrated panel can observe a presentation-friendly shell status without owning a second socket state machine.</done>
</task>

<task type="auto">
  <name>Task 2: Render compact bound-project identity and mismatch UI in the panel header</name>
  <files>src/components/shell/view/IntegratedTerminalPanel.tsx, src/i18n/locales/en/chat.json, src/i18n/locales/zh-CN/chat.json, src/i18n/locales/de/chat.json, src/i18n/locales/ja/chat.json, src/i18n/locales/ko/chat.json, src/i18n/locales/ru/chat.json</files>
  <read_first>src/components/shell/view/IntegratedTerminalPanel.tsx, .planning/phases/02-project-aware-terminal-launch/02-CONTEXT.md, src/i18n/locales/en/chat.json, src/i18n/locales/zh-CN/chat.json, src/i18n/locales/de/chat.json, src/i18n/locales/ja/chat.json, src/i18n/locales/ko/chat.json, src/i18n/locales/ru/chat.json</read_first>
  <action>Update `IntegratedTerminalPanel.tsx` so the header renders the fixed title `Terminal`, the bound project display name, a shortened bound project path derived from `terminalBinding.projectPath`, and a lightweight status chip driven by `ShellStatusSnapshot.phase`. Render a mismatch pill only when `currentProject?.name !== terminalBinding.projectName`, using the exact label pattern `Viewing {{projectName}}`. Keep the permanent action area limited to the existing hide/collapse control. Add the minimum new translation keys under `chat.shell.header` across `en`, `zh-CN`, `de`, `ja`, `ko`, and `ru`, reusing existing `tabs.shell` and `shell.connecting` keys where possible.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `terminalBinding.projectDisplayName`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains a comparison between `currentProject?.name` and `terminalBinding.projectName`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains a status chip derived from `shellStatus.phase`
    - `src/i18n/locales/en/chat.json` contains `shell.header.viewingProject`
  </acceptance_criteria>
  <done>The integrated panel clearly communicates terminal ownership and mismatch state without turning the header into a full control bar.</done>
</task>

<task type="auto">
  <name>Task 3: Replace fixed shell-body height with flex-based sizing that survives the richer header</name>
  <files>src/components/shell/view/IntegratedTerminalPanel.tsx</files>
  <read_first>src/components/shell/view/IntegratedTerminalPanel.tsx, src/components/standalone-shell/view/StandaloneShell.tsx, src/components/shell/view/Shell.tsx, src/components/shell/hooks/useShellTerminal.ts</read_first>
  <action>Remove the hard-coded `h-[calc(100%-49px)]` shell body wrapper from `IntegratedTerminalPanel.tsx` and replace it with a flex layout that uses `flex-1` and `min-h-0` for the shell region. Preserve the existing `h-full` / `min-h-0` chain through `StandaloneShell` and `Shell`, and keep the existing `ResizeObserver` + `fit()` + websocket `resize` path as the only terminal sizing mechanism. Do not add new websocket events or duplicate fit calculations in the panel header.</action>
  <verify>npm run lint && npm run typecheck</verify>
  <acceptance_criteria>
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` no longer contains `h-[calc(100%-49px)]`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains a shell body wrapper with both `flex-1` and `min-h-0`
    - `src/components/standalone-shell/view/StandaloneShell.tsx` still contains `min-h-0 w-full flex-1`
    - `src/components/shell/view/Shell.tsx` still contains `relative flex-1 overflow-hidden`
  </acceptance_criteria>
  <done>The integrated terminal viewport stays correctly sized even after the header gains extra identity and status content.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint && npm run typecheck
- [ ] Integrated terminal header always shows bound project identity
- [ ] Mismatch UI appears only when the viewed project differs from the bound terminal project
- [ ] Terminal viewport still fits correctly after header growth, reopen, and manual resize
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Bound terminal ownership is visible in the integrated UI
- Status/retry behavior stays lightweight and consistent with the existing shell runtime
- Richer header content does not break xterm viewport sizing
</success_criteria>

<output>
After completion, create `.planning/phases/02-project-aware-terminal-launch/02-02-SUMMARY.md`
</output>
