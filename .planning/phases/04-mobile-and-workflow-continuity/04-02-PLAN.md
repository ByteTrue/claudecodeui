---
phase: 04-mobile-and-workflow-continuity
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/components/shell/view/IntegratedTerminalPanel.tsx
  - src/components/shell/view/StandaloneShell.tsx
  - src/components/shell/view/Shell.tsx
  - src/components/shell/hooks/useShellTerminal.ts
  - src/components/shell/hooks/useMobileTerminalSheet.ts
autonomous: true
requirements:
  - MOBL-01
  - MOBL-02
user_setup: []
must_haves:
  truths:
    - On mobile, the integrated terminal opens as a bottom sheet that leaves the rest of the workspace reachable instead of behaving like a desktop-height panel.
    - Software-keyboard and visual-viewport changes keep the terminal prompt/input area visible and usable.
    - Mobile terminal height can adapt by touch without disturbing the desktop resize contract.
  artifacts:
    - A dedicated mobile sheet controller derives height from `window.visualViewport?.height ?? window.innerHeight`.
    - `IntegratedTerminalPanel` uses fixed mobile positioning above the mobile nav safe area and a mobile drag handle.
    - `Shell` / `useShellTerminal` receive mobile awareness for focus and keyboard-open viewport handling.
  key_links:
    - `IntegratedTerminalPanel` consumes a mobile sheet hook for height/drag behavior while preserving the existing desktop height path.
    - `StandaloneShell` and `Shell` pass `isMobile` down to the terminal runtime.
    - `useShellTerminal` reacts to mobile focus/viewport behavior without replacing the existing xterm fit pipeline.
---

<objective>
Implement mobile-specific panel, viewport, and keyboard behavior for the integrated terminal.

Purpose: Replace the current desktop-first mobile panel behavior with a real phone-friendly terminal sheet that remains usable when the software keyboard and browser chrome change the viewport.
Output: A visual-viewport-aware mobile terminal sheet with touch drag, safe-area positioning, and usable focused input behavior.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mobile-and-workflow-continuity/04-RESEARCH.md
@.planning/phases/04-mobile-and-workflow-continuity/04-VALIDATION.md
@src/components/shell/view/IntegratedTerminalPanel.tsx
@src/components/standalone-shell/view/StandaloneShell.tsx
@src/components/shell/view/Shell.tsx
@src/components/shell/hooks/useShellTerminal.ts
@src/components/quick-settings-panel/hooks/useQuickSettingsDrag.ts
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a mobile terminal-sheet controller instead of reusing desktop persisted height</name>
  <files>src/components/shell/hooks/useMobileTerminalSheet.ts, src/components/shell/view/IntegratedTerminalPanel.tsx</files>
  <read_first>src/components/shell/view/IntegratedTerminalPanel.tsx, src/components/quick-settings-panel/hooks/useQuickSettingsDrag.ts, src/index.css, .planning/phases/04-mobile-and-workflow-continuity/04-RESEARCH.md</read_first>
  <action>Create `src/components/shell/hooks/useMobileTerminalSheet.ts` and use it from `IntegratedTerminalPanel.tsx`. The hook must derive viewport height from the exact expression `window.visualViewport?.height ?? window.innerHeight`, clamp the mobile sheet height between `260` and `viewportHeight - 88`, and use `Math.round(viewportHeight * 0.62)` as the default open height when no previous mobile height exists. Persist the mobile value under a dedicated storage key such as `terminalPanelMobileHeight` so desktop `terminalPanelHeight` remains unchanged. In `IntegratedTerminalPanel.tsx`, keep the current desktop flex panel path intact, but when `isMobile` is true render the panel as a fixed bottom sheet positioned above the mobile nav using `bottom: 'var(--mobile-nav-total)'`, `left: 0`, and `right: 0`, and apply the mobile hook's `sheetHeight` / `maxHeight` instead of the desktop `height` prop.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/components/shell/hooks/useMobileTerminalSheet.ts` exists
    - `src/components/shell/hooks/useMobileTerminalSheet.ts` contains `window.visualViewport?.height ?? window.innerHeight`
    - `src/components/shell/hooks/useMobileTerminalSheet.ts` contains `Math.round(viewportHeight * 0.62)`
    - `src/components/shell/hooks/useMobileTerminalSheet.ts` contains `terminalPanelMobileHeight`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `useMobileTerminalSheet`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `bottom: 'var(--mobile-nav-total)'`
  </acceptance_criteria>
  <done>Mobile terminal height is now driven by a viewport-aware sheet model instead of the desktop resize state.</done>
</task>

<task type="auto">
  <name>Task 2: Keep terminal input usable while the software keyboard changes the viewport</name>
  <files>src/components/shell/view/IntegratedTerminalPanel.tsx, src/components/standalone-shell/view/StandaloneShell.tsx, src/components/shell/view/Shell.tsx, src/components/shell/hooks/useShellTerminal.ts</files>
  <read_first>src/components/shell/view/IntegratedTerminalPanel.tsx, src/components/standalone-shell/view/StandaloneShell.tsx, src/components/shell/view/Shell.tsx, src/components/shell/hooks/useShellTerminal.ts, .planning/phases/04-mobile-and-workflow-continuity/04-RESEARCH.md</read_first>
  <action>Thread an explicit `isMobile` prop from `IntegratedTerminalPanel.tsx` through `StandaloneShell.tsx` into `Shell.tsx` and `useShellTerminal.ts`. In the mobile sheet controller, listen to `visualViewport` `resize` and `scroll` events and recompute the sheet bounds whenever the software keyboard changes the viewport. When the xterm helper textarea receives focus on mobile, ensure the open sheet height is promoted to at least `Math.max(320, Math.round(viewportHeight * 0.55))`. In `useShellTerminal.ts`, when `isMobile` is true and the terminal gains focus or initializes, call `terminalContainerRef.current?.scrollIntoView({ block: 'nearest' })` after the xterm textarea is available so the prompt stays in view. Preserve the existing `ResizeObserver` + `fit()` + websocket `resize` pipeline as the only terminal sizing mechanism.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/components/standalone-shell/view/StandaloneShell.tsx` contains `isMobile`
    - `src/components/shell/view/Shell.tsx` contains `isMobile`
    - `src/components/shell/hooks/useShellTerminal.ts` contains `scrollIntoView({ block: 'nearest' })`
    - `src/components/shell/hooks/useMobileTerminalSheet.ts` contains `visualViewport.addEventListener('resize'`
    - `src/components/shell/hooks/useMobileTerminalSheet.ts` contains `visualViewport.addEventListener('scroll'`
    - `src/components/shell/hooks/useMobileTerminalSheet.ts` contains `Math.max(320, Math.round(viewportHeight * 0.55))`
  </acceptance_criteria>
  <done>Mobile keyboard-open states keep the active prompt and input area visible without abandoning the existing fit pipeline.</done>
</task>

<task type="auto">
  <name>Task 3: Add a touch drag handle for the mobile terminal sheet</name>
  <files>src/components/shell/hooks/useMobileTerminalSheet.ts, src/components/shell/view/IntegratedTerminalPanel.tsx</files>
  <read_first>src/components/shell/hooks/useMobileTerminalSheet.ts, src/components/quick-settings-panel/hooks/useQuickSettingsDrag.ts, src/components/shell/view/IntegratedTerminalPanel.tsx</read_first>
  <action>Using the drag thresholds and body-scroll-lock pattern from `useQuickSettingsDrag.ts`, add touch-resize support to the new mobile terminal sheet. Expose `startDrag`, `endDrag`, `isDragging`, and the current `sheetHeight` from `useMobileTerminalSheet.ts`. In `IntegratedTerminalPanel.tsx`, render a visible mobile grab handle above the header and wire it to `onTouchStart` plus document-level `touchmove` / `touchend` listeners through the hook. The drag range must clamp to the same min/max values as the hook, must not affect the existing desktop mouse resize strip, and must keep body scrolling locked while a touch drag is active.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/components/shell/hooks/useMobileTerminalSheet.ts` contains `touchmove`
    - `src/components/shell/hooks/useMobileTerminalSheet.ts` contains `touchend`
    - `src/components/shell/hooks/useMobileTerminalSheet.ts` contains `document.body.style.overflow = 'hidden'`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `onTouchStart`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains a mobile grab handle branch with `isMobile`
  </acceptance_criteria>
  <done>The mobile terminal can now be resized by touch as a sheet instead of relying on the desktop row-resize interaction.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint && npm run typecheck
- [ ] On mobile, the terminal opens above the mobile nav as a sheet instead of reusing desktop panel layout
- [ ] Focusing terminal input with the software keyboard keeps the prompt/input area visible
- [ ] The mobile sheet can be resized by touch without breaking the rest of the workspace
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Mobile panel behavior no longer depends on desktop height assumptions
- Keyboard-open viewport changes keep the terminal usable
- Mobile users can resize the terminal sheet by touch
</success_criteria>

<output>
After completion, create `.planning/phases/04-mobile-and-workflow-continuity/04-02-SUMMARY.md`
</output>
