---
phase: 04-mobile-and-workflow-continuity
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - src/components/shell/view/IntegratedTerminalPanel.tsx
  - src/components/standalone-shell/view/StandaloneShell.tsx
  - src/components/shell/view/Shell.tsx
  - src/components/shell/view/subcomponents/TerminalShortcutsPanel.tsx
  - src/components/shell/view/subcomponents/ShellMinimalView.tsx
  - src/i18n/locales/en/chat.json
  - src/i18n/locales/zh-CN/chat.json
  - src/i18n/locales/de/chat.json
  - src/i18n/locales/ja/chat.json
  - src/i18n/locales/ko/chat.json
  - src/i18n/locales/ru/chat.json
autonomous: true
requirements:
  - MOBL-03
  - MOBL-04
user_setup: []
must_haves:
  truths:
    - Mobile terminal tabs and actions are large enough and placed predictably enough for touch use.
    - Mobile shortcut/help controls live inside the terminal sheet instead of as a desktop-style fixed side drawer.
    - The latest terminal output remains readable even when mobile auxiliary controls are present.
  artifacts:
    - `IntegratedTerminalPanel` renders a mobile-specific compact header/tab strip with touch-friendly targets.
    - `TerminalShortcutsPanel` has a mobile in-panel shortcut tray and preserves the current desktop drawer for non-mobile layouts.
    - `Shell` / `ShellMinimalView` add mobile-safe bottom padding and overlay placement so controls do not cover the newest output.
  key_links:
    - `IntegratedTerminalPanel` passes `isMobile` into the shell stack and uses it to branch mobile tab chrome.
    - `Shell` passes `isMobile` to `TerminalShortcutsPanel` and mobile overlays.
    - `ShellMinimalView` and the CLI prompt overlay reserve space for the mobile shortcut tray/safe area.
---

<objective>
Adapt tab controls and output presentation for touch-first terminal use on mobile.

Purpose: Finish the Phase 4 mobile usability layer by replacing desktop-density controls with touch-sized terminal chrome and by keeping the newest output readable while mobile helper controls are present.
Output: A touch-friendly mobile tab/header system, in-panel mobile shortcut tray, and readable output/overlay placement on phones.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mobile-and-workflow-continuity/04-RESEARCH.md
@.planning/phases/04-mobile-and-workflow-continuity/04-VALIDATION.md
@src/components/shell/view/IntegratedTerminalPanel.tsx
@src/components/standalone-shell/view/StandaloneShell.tsx
@src/components/shell/view/Shell.tsx
@src/components/shell/view/subcomponents/TerminalShortcutsPanel.tsx
@src/components/shell/view/subcomponents/ShellMinimalView.tsx
@src/i18n/locales/en/chat.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the mobile terminal header and tab strip touch-friendly</name>
  <files>src/components/shell/view/IntegratedTerminalPanel.tsx, src/i18n/locales/en/chat.json, src/i18n/locales/zh-CN/chat.json, src/i18n/locales/de/chat.json, src/i18n/locales/ja/chat.json, src/i18n/locales/ko/chat.json, src/i18n/locales/ru/chat.json</files>
  <read_first>src/components/shell/view/IntegratedTerminalPanel.tsx, src/components/app/MobileNav.tsx, .planning/phases/04-mobile-and-workflow-continuity/04-RESEARCH.md, src/i18n/locales/en/chat.json</read_first>
  <action>In `src/components/shell/view/IntegratedTerminalPanel.tsx`, add an explicit mobile branch for header and tab-strip density. Keep the fixed title `Terminal`, but on mobile collapse the project identity into a compact second line, keep the action buttons icon-first, and give every interactive control touch sizing with exact utilities such as `touch-manipulation`, `h-10`, `min-w-[140px]`, and `px-3`. Make the tab strip horizontally scrollable with `overflow-x-auto`, `snap-x`, and stable spacing so create/switch/close/restart remain reachable with the keyboard open. Preserve the current desktop layout for non-mobile mode. Add any newly required shell mobile labels across `en`, `zh-CN`, `de`, `ja`, `ko`, and `ru` under the existing `chat.shell` namespace, and use `defaultValue` fallbacks if a new key would otherwise block the mobile layout branch.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `touch-manipulation`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `snap-x`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `min-w-[140px]`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `isMobile ?`
    - `src/i18n/locales/en/chat.json` contains `shell.tabs.newTab`
    - `src/i18n/locales/zh-CN/chat.json` contains `shell.tabs.closeTab`
    - `src/i18n/locales/de/chat.json` contains `shell.header.statusLive`
    - `src/i18n/locales/ja/chat.json` contains `shell.tabs.restartTab`
    - `src/i18n/locales/ko/chat.json` contains `shell.header.statusDisconnected`
    - `src/i18n/locales/ru/chat.json` contains `shell.header.statusExited`
  </acceptance_criteria>
  <done>Mobile terminal tabs and core actions are now sized and arranged for thumb use instead of desktop pointer density.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the desktop fixed shortcut drawer with a mobile in-panel shortcut tray</name>
  <files>src/components/shell/view/Shell.tsx, src/components/shell/view/subcomponents/TerminalShortcutsPanel.tsx</files>
  <read_first>src/components/shell/view/Shell.tsx, src/components/shell/view/subcomponents/TerminalShortcutsPanel.tsx, .planning/phases/04-mobile-and-workflow-continuity/04-RESEARCH.md</read_first>
  <action>Thread `isMobile` from `Shell.tsx` into `TerminalShortcutsPanel.tsx`. Preserve the current fixed right-side drawer implementation exactly for non-mobile mode. For mobile mode, replace that drawer with an in-panel shortcut tray that renders inside the terminal surface near the bottom, uses `absolute inset-x-0 bottom-0`, `overflow-x-auto`, and touch-sized buttons, and exposes the existing actions for `Esc`, `Tab`, `Shift+Tab`, arrow navigation, and scroll-to-bottom. Remove the fixed right-edge pull tab entirely from the mobile branch. Keep all shortcut actions wired through the same `sendSocketMessage` / `terminalRef.current?.scrollToBottom()` behavior.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/components/shell/view/Shell.tsx` contains `isMobile={isMobile}`
    - `src/components/shell/view/subcomponents/TerminalShortcutsPanel.tsx` contains `isMobile`
    - `src/components/shell/view/subcomponents/TerminalShortcutsPanel.tsx` contains `absolute inset-x-0 bottom-0`
    - `src/components/shell/view/subcomponents/TerminalShortcutsPanel.tsx` contains `overflow-x-auto`
    - `src/components/shell/view/subcomponents/TerminalShortcutsPanel.tsx` still contains `sendSocketMessage`
    - `src/components/shell/view/subcomponents/TerminalShortcutsPanel.tsx` still contains `scrollToBottom`
  </acceptance_criteria>
  <done>Mobile users get shortcut/help controls inside the terminal sheet instead of a desktop side drawer that wastes horizontal space.</done>
</task>

<task type="auto">
  <name>Task 3: Reserve space for mobile helper overlays so the newest output stays readable</name>
  <files>src/components/standalone-shell/view/StandaloneShell.tsx, src/components/shell/view/Shell.tsx, src/components/shell/view/subcomponents/ShellMinimalView.tsx</files>
  <read_first>src/components/standalone-shell/view/StandaloneShell.tsx, src/components/shell/view/Shell.tsx, src/components/shell/view/subcomponents/ShellMinimalView.tsx, src/components/shell/view/subcomponents/TerminalShortcutsPanel.tsx</read_first>
  <action>Thread `isMobile` through `StandaloneShell.tsx` into `Shell.tsx` and `ShellMinimalView.tsx`. In `Shell.tsx`, when `isMobile` is true, add bottom padding to the terminal viewport container so the shortcut tray and safe area do not cover the newest output. Change the CLI prompt option overlay to a horizontally scrollable mobile-safe layout with larger buttons and bottom padding that sits above the mobile shortcut tray. In `ShellMinimalView.tsx`, move the auth URL panel and the "Show login URL" toggle upward using a bottom offset like `calc(var(--mobile-nav-total) + 56px)` so they do not collide with the mobile shortcut tray or safe area. Preserve existing desktop overlay placement and keep the underlying shell runtime untouched.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/components/standalone-shell/view/StandaloneShell.tsx` contains `isMobile`
    - `src/components/shell/view/Shell.tsx` contains `pb-[calc(var(--mobile-nav-total)`
    - `src/components/shell/view/Shell.tsx` contains `overflow-x-auto`
    - `src/components/shell/view/Shell.tsx` contains a mobile branch for the CLI prompt overlay with `isMobile`
    - `src/components/shell/view/subcomponents/ShellMinimalView.tsx` contains `calc(var(--mobile-nav-total) + 56px)`
    - `src/components/shell/view/subcomponents/ShellMinimalView.tsx` still contains `md:hidden`
  </acceptance_criteria>
  <done>Mobile helper controls no longer sit on top of the newest output, so output remains readable while touch controls stay accessible.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint && npm run typecheck
- [ ] On mobile, tab create/switch/close/restart controls are touch-reachable and remain usable with the keyboard open
- [ ] Mobile shortcut controls render inside the terminal sheet instead of as a fixed side drawer
- [ ] The newest output lines remain readable while mobile helper controls and auth overlays are visible
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Mobile tab and action controls are touch-friendly
- Mobile shortcut/help controls live inside the terminal sheet
- Output remains readable even when mobile helper overlays are present
</success_criteria>

<output>
After completion, create `.planning/phases/04-mobile-and-workflow-continuity/04-03-SUMMARY.md`
</output>
