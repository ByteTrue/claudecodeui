---
phase: 04-mobile-and-workflow-continuity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/main-content/view/MainContent.tsx
  - src/components/main-content/view/subcomponents/MainContentStateView.tsx
  - src/components/shell/view/IntegratedTerminalPanel.tsx
  - src/components/shell/view/Shell.tsx
autonomous: true
requirements:
  - SESS-06
user_setup: []
must_haves:
  truths:
    - Normal workspace navigation and panel hide/show do not destroy the active integrated terminal host while tabs still exist.
    - The integrated panel can still reopen to the same active tab even when the current workspace surface is in loading or empty-project state.
    - A hidden-but-mounted terminal leaves no floating shortcut chrome or focus traps on screen.
  artifacts:
    - `MainContent` renders workspace loading/empty states and `IntegratedTerminalPanel` from the same layout pass instead of returning early before the panel.
    - `IntegratedTerminalPanel` uses `isOpen` as a visibility flag and keeps `StandaloneShell` mounted while an active tab exists.
    - `Shell` gates prompt overlays and `TerminalShortcutsPanel` on `isActive` so a hidden terminal stays quiet on screen.
  key_links:
    - `useProjectsState` visibility state still flows through `MainContent` into `IntegratedTerminalPanel`.
    - `MainContent` derives `boundProject` and `boundSession` from `activeTerminalTab` even when the workspace body is showing a state view.
    - `IntegratedTerminalPanel` passes `isActive={isOpen}` into `StandaloneShell` / `Shell` while preserving the mounted tab identity.
---

<objective>
Preserve terminal session continuity across normal workspace navigation and panel visibility changes within the same browser session.

Purpose: Remove the remaining UI lifecycle gaps where the integrated terminal disappears or resets because the workspace body changes state or the panel is temporarily hidden.
Output: A continuity-safe terminal host that survives loading/empty view transitions, hide/reopen, and same-session view switching without leaving stray shell UI behind.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mobile-and-workflow-continuity/04-RESEARCH.md
@.planning/phases/04-mobile-and-workflow-continuity/04-VALIDATION.md
@.planning/phases/03-multi-session-terminal-tabs/03-VERIFICATION.md
@src/components/main-content/view/MainContent.tsx
@src/components/main-content/view/subcomponents/MainContentStateView.tsx
@src/components/shell/view/IntegratedTerminalPanel.tsx
@src/components/shell/view/Shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the terminal panel outside workspace loading and empty-state short circuits</name>
  <files>src/components/main-content/view/MainContent.tsx, src/components/main-content/view/subcomponents/MainContentStateView.tsx</files>
  <read_first>src/components/main-content/view/MainContent.tsx, src/components/main-content/view/subcomponents/MainContentStateView.tsx, .planning/phases/04-mobile-and-workflow-continuity/04-RESEARCH.md, .planning/phases/03-multi-session-terminal-tabs/03-VERIFICATION.md</read_first>
  <action>In `src/components/main-content/view/MainContent.tsx`, replace the current early `return` branches for `isLoading` and `!selectedProject` with a `workspaceContent` branch or equivalent local variable. Keep the header and outer `flex min-h-0 flex-1 flex-col` layout mounted in all cases. When loading, render `MainContentStateView mode="loading"` inside the workspace body; when no project is selected, render `MainContentStateView mode="empty"` inside the workspace body; otherwise render the existing chat/files/git/tasks/plugin/editor split. After that shared workspace body, always render `IntegratedTerminalPanel` from the same component tree so the panel can remain available when `terminalPanelState.tabs.length > 0`. Do not move terminal state into `MainContentStateView`; keep `boundProject` and `boundSession` derived from `activeTerminalTab` in `MainContent` exactly as the source of terminal identity.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/components/main-content/view/MainContent.tsx` contains `const workspaceContent =`
    - `src/components/main-content/view/MainContent.tsx` still contains `IntegratedTerminalPanel`
    - `src/components/main-content/view/MainContent.tsx` no longer contains `return <MainContentStateView mode="loading"`
    - `src/components/main-content/view/MainContent.tsx` no longer contains `return <MainContentStateView mode="empty"`
    - `src/components/main-content/view/MainContent.tsx` still derives `activeTerminalTab = terminalPanelState.tabs.find`
  </acceptance_criteria>
  <done>The integrated terminal is no longer implicitly destroyed just because the workspace body is showing a loading or empty state.</done>
</task>

<task type="auto">
  <name>Task 2: Separate panel visibility from mounted shell lifecycle</name>
  <files>src/components/shell/view/IntegratedTerminalPanel.tsx</files>
  <read_first>src/components/shell/view/IntegratedTerminalPanel.tsx, .planning/phases/04-mobile-and-workflow-continuity/04-RESEARCH.md, .planning/phases/01-integrated-terminal-panel/01-CONTEXT.md</read_first>
  <action>In `src/components/shell/view/IntegratedTerminalPanel.tsx`, stop returning `null` when `isOpen` is false. Keep the existing `activeTab` and `boundProject` guards, but treat `isOpen` as visibility only. Use the exact height rule `style={{ height: isOpen ? height : 0 }}` on the outer panel wrapper. When `isOpen` is false, add closed-state classes that remove border/shadow interaction from the wrapper and make the visible shell region non-interactive, such as `pointer-events-none`, `border-t-transparent`, and `shadow-none`. Keep the `StandaloneShell` subtree mounted whenever `activeTab` and `boundProject` exist, wrap the shell body with `aria-hidden={!isOpen}`, and preserve `isActive={isOpen}` on the mounted shell so focus behavior still follows visibility. Do not change the Phase 3 tab identity contract or the active tab selection logic.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` no longer contains `|| !isOpen` in the top-level null guard
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `style={{ height: isOpen ? height : 0 }}`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `aria-hidden={!isOpen}`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` still passes `isActive={isOpen}`
    - `src/components/shell/view/IntegratedTerminalPanel.tsx` contains `pointer-events-none`
  </acceptance_criteria>
  <done>Panel hide/show becomes a visual transition instead of a forced shell-host teardown.</done>
</task>

<task type="auto">
  <name>Task 3: Silence shell overlays and shortcut chrome while the hidden panel remains mounted</name>
  <files>src/components/shell/view/Shell.tsx</files>
  <read_first>src/components/shell/view/Shell.tsx, src/components/shell/view/subcomponents/TerminalShortcutsPanel.tsx, .planning/phases/04-mobile-and-workflow-continuity/04-RESEARCH.md</read_first>
  <action>In `src/components/shell/view/Shell.tsx`, treat `isActive` as the contract for all floating shell UI. Keep the runtime mounted, but change the CLI prompt overlay guard to the exact condition `cliPromptOptions && isConnected && isActive`. Also gate `TerminalShortcutsPanel` behind `isActive` so a closed-but-mounted integrated panel does not leave the fixed shortcut handle or drawer on screen. Preserve the existing xterm container mount, websocket lifecycle, and `isActive`-based focus effect. Do not reintroduce any new reconnect logic here; this task is only about keeping the hidden terminal quiet while continuity is preserved.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/components/shell/view/Shell.tsx` contains `cliPromptOptions && isConnected && isActive`
    - `src/components/shell/view/Shell.tsx` contains `isActive && (`
    - `src/components/shell/view/Shell.tsx` still contains `TerminalShortcutsPanel`
    - `src/components/shell/view/Shell.tsx` still contains `if (!isActive || !isInitialized || !isConnected)`
  </acceptance_criteria>
  <done>The hidden integrated terminal stays mounted for continuity without leaking visible shell UI into the rest of the workspace.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint && npm run typecheck
- [ ] Hiding and reopening the panel keeps the same active terminal tab and local terminal view instead of remounting a fresh shell host
- [ ] Switching between chat/files/git and the empty/loading workspace states does not prevent reopening the same terminal panel
- [ ] A hidden terminal no longer leaves the shortcuts handle or CLI prompt overlay on screen
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Terminal continuity no longer depends solely on backend PTY replay during normal same-session view changes
- Loading/empty workspace states can coexist with an active integrated terminal
- Hidden-but-mounted terminals do not leak floating shell UI
</success_criteria>

<output>
After completion, create `.planning/phases/04-mobile-and-workflow-continuity/04-01-SUMMARY.md`
</output>
