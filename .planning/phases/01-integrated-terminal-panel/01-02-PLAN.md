---
phase: 01-integrated-terminal-panel
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/hooks/useProjectsState.ts
  - src/components/main-content/view/MainContent.tsx
  - src/components/main-content/view/subcomponents/MainContentTabSwitcher.tsx
  - src/components/main-content/view/subcomponents/MainContentHeader.tsx
  - src/components/main-content/view/subcomponents/MainContentTitle.tsx
  - src/components/main-content/types/types.ts
  - src/components/app/AppContent.tsx
  - src/components/app/MobileNav.tsx
  - src/components/shell/view/IntegratedTerminalPanel.tsx
autonomous: true
requirements:
  - INTE-01
  - INTE-02
user_setup: []
must_haves:
  truths:
    - Reusing the existing shell control opens or focuses the bottom terminal panel without changing the active workspace view.
    - Users can collapse and reopen the panel while staying in the same workspace flow.
    - If the panel is open, switching between main workspace views does not close it.
    - The old full-page shell behavior is no longer the primary navigation model.
  artifacts:
    - Main workspace trigger components route shell actions through the panel state rather than `activeTab === 'shell'`.
    - The integrated panel exposes collapse and reopen behavior through its panel chrome.
    - Persisted or legacy `shell` tab state no longer traps the app in a removed page model.
  key_links:
    - MainContentTabSwitcher invokes panel open/focus behavior instead of switching the workspace to a shell page.
    - AppContent/MainContent propagate terminal panel control callbacks through the existing workspace shell chrome.
    - Collapse/reopen controls in IntegratedTerminalPanel update shared panel state and preserve the current active workspace tab.
    - Main workspace tab changes preserve terminal panel visibility when the panel is already open.
---

<objective>
Rewire workspace shell access from page navigation into panel open, focus, collapse, and reopen behavior.

Purpose: Deliver the actual user-facing “open terminal from the current workspace” flow and remove the old dedicated-shell mental model.
Output: Trigger wiring through existing shell controls, collapse/reopen behavior, and legacy tab-state handling that keeps the workspace stable.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-integrated-terminal-panel/01-CONTEXT.md
@.planning/phases/01-integrated-terminal-panel/01-RESEARCH.md
@src/hooks/useProjectsState.ts
@src/components/app/AppContent.tsx
@src/components/app/MobileNav.tsx
@src/components/main-content/view/MainContent.tsx
@src/components/main-content/view/subcomponents/MainContentHeader.tsx
@src/components/main-content/view/subcomponents/MainContentTabSwitcher.tsx
@src/components/main-content/view/subcomponents/MainContentTitle.tsx
@src/components/shell/view/IntegratedTerminalPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reinterpret the shell trigger as panel open or focus</name>
  <files>src/components/app/AppContent.tsx, src/components/main-content/view/MainContent.tsx, src/components/main-content/view/subcomponents/MainContentTabSwitcher.tsx, src/components/main-content/types/types.ts</files>
  <action>Thread explicit terminal panel open/focus callbacks through the workspace chrome so the existing shell control opens the bottom panel without changing `activeTab`. The panel trigger should bring the terminal forward if it is already open. Keep this behavior centered on desktop/main workspace chrome in this phase; do not let it drift into mobile-specific feature work beyond keeping existing trigger surfaces compilable.</action>
  <verify>npm run lint</verify>
  <done>The existing shell control path can open or focus the integrated panel while leaving the current workspace view active.</done>
</task>

<task type="auto">
  <name>Task 2: Implement collapse and reopen behavior around shared panel state</name>
  <files>src/hooks/useProjectsState.ts, src/components/main-content/view/MainContent.tsx, src/components/shell/view/IntegratedTerminalPanel.tsx</files>
  <action>Add explicit collapse and reopen behavior tied to the shared terminal panel state. Collapsing must hide the panel fully, not leave a partial rail, and reopening must restore the panel within the current workspace flow instead of routing the app elsewhere. Make the plan explicit that when the panel is already open, switching between main workspace views does not close it. Keep the terminal mounted/managed in a way that supports the later continuity phases rather than forcing a full page-model reset.</action>
  <verify>npm run lint</verify>
  <done>The panel can be collapsed and reopened from within the workspace without breaking the active workspace context, and open-panel state survives switching between main workspace views.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the full-page shell destination model cleanly</name>
  <files>src/hooks/useProjectsState.ts, src/components/main-content/view/MainContent.tsx, src/components/app/MobileNav.tsx, src/components/main-content/view/subcomponents/MainContentTitle.tsx</files>
  <action>Remove the assumption that `shell` is a dedicated full-page destination in the main workspace. Handle legacy persisted `activeTab === 'shell'` safely, update workspace title/chrome behavior so it still makes sense without a shell page, and ensure the app no longer depends on a separate shell-only view for Phase 1 success.</action>
  <verify>npm run lint</verify>
  <done>The workspace no longer depends on a dedicated shell page model, and legacy shell-tab state cannot strand the app in an invalid navigation path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint
- [ ] Shell trigger opens or focuses the bottom panel without changing activeTab
- [ ] Collapse and reopen keep the user in the same workspace flow
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Shell access is panel-based rather than page-based
- Collapse/reopen behavior is shared-state driven and workspace-safe
- Open-panel state survives switching between main workspace views
</success_criteria>

<output>
After completion, create `.planning/phases/01-integrated-terminal-panel/01-02-SUMMARY.md`
</output>
