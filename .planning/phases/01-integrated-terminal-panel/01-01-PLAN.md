---
phase: 01-integrated-terminal-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/app.ts
  - src/hooks/useProjectsState.ts
  - src/components/main-content/view/MainContent.tsx
  - src/components/main-content/types/types.ts
  - src/components/shell/view/IntegratedTerminalPanel.tsx
autonomous: true
requirements:
  - INTE-01
user_setup: []
must_haves:
  truths:
    - The workspace can render an integrated terminal panel without switching to a dedicated shell page.
    - The integrated terminal panel uses the existing shell runtime instead of a second terminal implementation.
    - The integrated terminal panel spans the full workspace width, including the editor area, rather than only the left content pane.
  artifacts:
    - src/components/shell/view/IntegratedTerminalPanel.tsx exists and wraps the current shell surface for panel use.
    - src/hooks/useProjectsState.ts stores explicit terminal panel visibility state separate from activeTab.
    - src/components/main-content/view/MainContent.tsx renders a shared bottom-panel region in the workspace layout.
  key_links:
    - MainContent reads terminal panel state from useProjectsState and mounts IntegratedTerminalPanel inside the workspace layout.
    - IntegratedTerminalPanel passes the current project and session through to the existing shell runtime surface.
    - The shared panel host is placed below the combined workspace content/editor region rather than inside only one pane.
---

<objective>
Establish the workspace-integrated terminal panel host and state model.

Purpose: Create the structural foundation for a bottom terminal panel without changing shell transport behavior or leaking into later-phase session features.
Output: A new integrated panel component, explicit panel visibility state, and a shared workspace layout region that can host the existing shell surface.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-integrated-terminal-panel/01-CONTEXT.md
@.planning/phases/01-integrated-terminal-panel/01-RESEARCH.md
@src/hooks/useProjectsState.ts
@src/types/app.ts
@src/components/main-content/view/MainContent.tsx
@src/components/main-content/types/types.ts
@src/components/standalone-shell/view/StandaloneShell.tsx
@src/components/shell/view/Shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit terminal panel workspace state</name>
  <files>src/types/app.ts, src/hooks/useProjectsState.ts</files>
  <action>Add a dedicated terminal panel state model that lives alongside the existing workspace state instead of piggybacking on `activeTab`. Include at minimum open/closed state and the minimal shape needed for later panel sizing, but do not add multi-session or project-cwd concerns from later phases. Preserve current persisted tab behavior for non-shell tabs and avoid introducing a second source of truth for main workspace navigation.</action>
  <verify>npm run lint</verify>
  <done>`useProjectsState` exposes terminal panel state/setters that are separate from `activeTab`, and type definitions reflect the new panel state cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create the integrated terminal panel host component</name>
  <files>src/components/shell/view/IntegratedTerminalPanel.tsx</files>
  <action>Create a panel-oriented wrapper around the existing shell surface for bottom-panel use. Reuse the current shell runtime through `StandaloneShell` or `Shell` instead of creating a new terminal implementation. Keep this first version focused on hosting the shell inside a workspace panel; do not add tab-management or project-launch features from later phases.</action>
  <verify>npm run lint</verify>
  <done>`IntegratedTerminalPanel.tsx` exists, uses the current shell feature stack, and provides a clear host surface for open/collapse behavior inside the workspace.</done>
</task>

<task type="auto">
  <name>Task 3: Mount a shared bottom-panel region in the workspace layout</name>
  <files>src/components/main-content/view/MainContent.tsx, src/components/main-content/types/types.ts, src/components/shell/view/IntegratedTerminalPanel.tsx</files>
  <action>Refactor `MainContent` so the terminal can live in a shared bottom region below the current workspace content instead of being rendered only as `activeTab === 'shell'`. Make the host explicitly span the full workspace width, including the editor area, rather than attaching only to the left content pane. Keep the main workspace content and editor/sidebar structure intact, and ensure the new panel receives the selected project/session inputs that the current full-page shell view uses today.</action>
  <verify>npm run lint</verify>
  <done>MainContent can render a full-width bottom terminal panel region without replacing the active workspace view, and the integrated panel is wired to the current shell surface with the existing project/session inputs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint
- [ ] The workspace layout contains a dedicated bottom-panel host for terminal rendering
- [ ] The terminal panel state is separate from `activeTab`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The workspace has a reusable bottom-panel terminal host
- Existing shell runtime reuse is preserved
- The panel host spans the full workspace width, including the editor area
</success_criteria>

<output>
After completion, create `.planning/phases/01-integrated-terminal-panel/01-01-SUMMARY.md`
</output>
