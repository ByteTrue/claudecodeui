---
phase: 01-integrated-terminal-panel
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/hooks/useProjectsState.ts
  - src/components/shell/view/IntegratedTerminalPanel.tsx
  - src/components/shell/view/Shell.tsx
  - src/components/shell/hooks/useShellTerminal.ts
  - src/components/code-editor/hooks/useEditorSidebar.ts
  - src/components/main-content/view/MainContent.tsx
autonomous: true
requirements:
  - INTE-03
user_setup: []
must_haves:
  truths:
    - Users can resize the integrated terminal panel height on desktop with an explicit grab affordance.
    - Reopening the panel restores the last chosen desktop height instead of resetting every time.
    - Terminal layout remains visually integrated and correctly fitted after panel resize/open changes.
  artifacts:
    - Integrated panel UI exposes a visible resize handle.
    - Shared workspace state stores and restores the remembered desktop panel height.
    - Terminal fitting logic is triggered correctly when panel height changes.
  key_links:
    - Panel drag interactions update shared panel height state that MainContent uses for layout.
    - Height changes trigger terminal fit/resize behavior through the existing shell runtime.
    - Reopen behavior reuses stored height instead of falling back to a fixed fresh size after every close.
---

<objective>
Add desktop resize behavior and final panel integration polish for the bottom terminal.

Purpose: Complete the phase contract by making the panel comfortably resizable, visually integrated, and stable when reopened.
Output: Desktop drag-resize behavior, remembered panel height, and terminal refit/polish aligned with the workspace layout.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-integrated-terminal-panel/01-CONTEXT.md
@.planning/phases/01-integrated-terminal-panel/01-RESEARCH.md
@src/hooks/useProjectsState.ts
@src/components/main-content/view/MainContent.tsx
@src/components/shell/view/IntegratedTerminalPanel.tsx
@src/components/shell/view/Shell.tsx
@src/components/shell/hooks/useShellTerminal.ts
@src/components/code-editor/hooks/useEditorSidebar.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement explicit desktop panel resizing</name>
  <files>src/components/shell/view/IntegratedTerminalPanel.tsx, src/components/main-content/view/MainContent.tsx, src/components/code-editor/hooks/useEditorSidebar.ts</files>
  <action>Add a visible desktop resize affordance for the integrated terminal panel and implement drag handling that feels consistent with the repo's existing editor-resize interaction. Use a flexible but bounded height range that preserves workspace context above the panel. Keep this desktop-focused; do not pull in later mobile viewport behavior.</action>
  <verify>npm run lint</verify>
  <done>The bottom panel has a clear grab strip and can be resized vertically on desktop within sensible bounds.</done>
</task>

<task type="auto">
  <name>Task 2: Persist and restore last desktop panel height</name>
  <files>src/hooks/useProjectsState.ts, src/components/shell/view/IntegratedTerminalPanel.tsx, src/components/main-content/view/MainContent.tsx</files>
  <action>Persist the user's last chosen desktop panel height and restore it when the panel reopens. Keep the persistence lightweight and local; Phase 1 does not need full reload/session restoration semantics beyond honoring the chosen panel height. Make sure first-open still defaults to roughly one-third of the workspace height.</action>
  <verify>npm run lint</verify>
  <done>First open uses the default desktop height, and subsequent reopen restores the user's last chosen desktop size.</done>
</task>

<task type="auto">
  <name>Task 3: Refit terminal runtime and polish integrated layout behavior</name>
  <files>src/components/shell/hooks/useShellTerminal.ts, src/components/shell/view/Shell.tsx, src/components/shell/view/IntegratedTerminalPanel.tsx, src/components/main-content/view/MainContent.tsx</files>
  <action>Ensure the shell surface refits correctly after panel open, collapse, and drag-resize changes. Apply the minimum necessary chrome/layout polish so the panel feels intentionally integrated rather than like a pasted-in full-page shell. Do not broaden this into terminal hardening, copy/paste improvements, or reconnect behavior from later phases.</action>
  <verify>npm run lint && npm run typecheck</verify>
  <done>The terminal stays visually aligned after resize/open transitions, and the panel looks like part of the workspace rather than a separate screen stuffed into the page.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint && npm run typecheck
- [ ] Desktop panel resize works with an explicit handle
- [ ] Reopening restores the last chosen desktop height
- [ ] Terminal fit remains correct after resize and reopen
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Desktop resize behavior is explicit and remembered
- Panel polish stays inside Phase 1 scope
</success_criteria>

<output>
After completion, create `.planning/phases/01-integrated-terminal-panel/01-03-SUMMARY.md`
</output>
